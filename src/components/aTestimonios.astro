---
const testimonios = [
  {
    texto: "Por su profesionalismo, modelo educativo y atención personalizada, así como una educación, el buen ambiente para los aprendizajes así como la libertad de expresión de cada alumno.",
    autor: "Leonor Bautista",
  },
  {
    texto: "El Instituto Wallon me ha brindado las mejores herramientas para desarrollarme tanto académica como personalmente. Los profesores siempre están dispuestos a ayudar y el ambiente es inigualable.",
    autor: "Carlos Martínez",
  },
  {
    texto: "La experiencia de estudiar en Wallon ha sido muy enriquecedora. La metodología aplicada nos prepara para enfrentar cualquier reto en el futuro. Estoy muy contento con mi elección.",
    autor: "Ana López",
  }
];

let currentTestimonio = 0;

function cambiarTestimonio() {
  currentTestimonio = (currentTestimonio + 1) % testimonios.length;
  document.getElementById('testimonio-texto').textContent = testimonios[currentTestimonio].texto;
  document.getElementById('testimonio-autor').textContent = testimonios[currentTestimonio].autor;
  document.querySelectorAll('.indicator').forEach((el, index) => {
    el.classList.toggle('bg-green-500', index === currentTestimonio);
    el.classList.toggle('bg-white', index !== currentTestimonio);
  });
}

if (typeof window !== "undefined") {
  window.addEventListener('load', () => {
    setInterval(cambiarTestimonio, 8000);
  });
}
---

<div class="relative bg-cover bg-center h-screen" style="background-image: url('/bg-footer.svg');">
  <div class="absolute inset-0 flex flex-col items-center justify-center py-16">
    <div class="text-center">
      <h2 class="text-4xl font-bold text-white mt-8">Testimonios</h2>
      <div class="w-full max-w-4xl mt-8 px-4">
        <div class="bg-gray-900 bg-opacity-50 p-8 rounded-lg">
          <p id="testimonio-texto" class="text-white text-lg">"{testimonios[0].texto}"</p>
          <p id="testimonio-autor" class="mt-4 font-bold text-white">{testimonios[0].autor}</p>
        </div>
        <div class="flex justify-center space-x-2 mt-4">
          {testimonios.map((_, index) => (
            <div
              class={`w-3 h-3 rounded-full indicator ${index === 0 ? 'bg-green-500' : 'bg-white'}`}
            ></div>
          ))}
        </div>
      </div>
    </div>
  </div>
  <footer class="absolute bottom-0 left-0 right-0 bg-gray-800 py-8">
    <div class="container mx-auto text-center text-white">
      <div class="mb-4">Derechos Reservados © Instituto Wallon.</div>
      <div class="mb-4">NORMAS DE CONVIVENCIA | AVISO DE PRIVACIDAD</div>
      <div class="flex justify-center space-x-4">
        <div class="bg-white rounded-full w-10 h-10 flex items-center justify-center">
          <img src="/imgPortal.svg" alt="Facebook" class="w-6 h-6">
        </div>
        <div class="bg-white rounded-full w-10 h-10 flex items-center justify-center">
          <img src="/twitter.svg" alt="Twitter" class="w-6 h-6">
        </div>
        <div class="bg-white rounded-full w-10 h-10 flex items-center justify-center">
          <img src="/instagram.svg" alt="Instagram" class="w-6 h-6">
        </div>
        <div class="bg-white rounded-full w-10 h-10 flex items-center justify-center">
          <img src="/youtube.svg" alt="YouTube" class="w-6 h-6">
        </div>
        <div class="bg-white rounded-full w-10 h-10 flex items-center justify-center">
          <img src="/whatsapp.svg" alt="WhatsApp" class="w-6 h-6">
        </div>
      </div>
    </div>
  </footer>
</div>
